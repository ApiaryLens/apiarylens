# docker/

Docker Compose files and container definitions for the self-hosted deployment
path — this is ApiaryLens's primary, first-class deployment target (see
[AGENTS.md](../AGENTS.md)).

Empty for now — no services have been built yet, so there's nothing to
containerize. Once the first app exists under [`apps/`](../apps/), its Compose
service definition belongs here.
