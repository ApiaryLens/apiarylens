# packages/

Shared code consumed by more than one app in [`apps/`](../apps/) lives here (e.g. a
shared data model, a shared UI component library, a shared sync client).

Empty for now. Don't create a package here for something only one app uses —
that belongs inside the app itself until a second consumer exists.
